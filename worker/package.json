{
  "name": "airbeat-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create airbeat",
    "d1:init": "wrangler d1 execute airbeat --file=schema.sql",
    "kv:create": "wrangler kv namespace create CACHE",
    "r2:create": "wrangler r2 bucket create airbeat-audio"
  },
  "dependencies": {
    "ts-md5": "^2.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
